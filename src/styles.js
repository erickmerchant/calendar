export const _start = `
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    max-width: 100%;
    border-radius: 3px;
    font: inherit;
  }

  html {
    height: 100%;
  }
`

export const styles = {
  app: `
    display: grid;
    height: 100%;
    background-color: hsl(200, 5%, 60%);
    font: 24px/1.5 Consolas, monaco, monospace;
  `
}
